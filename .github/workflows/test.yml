name: Auto-test Subscribed Proxies

on:
  # 定时触发
  schedule:
    - cron: '0 */6 * * *'
  # 手动触发
  workflow_dispatch:

jobs:
  fetch-files:
    runs-on: windows-latest

    steps:
      - name: Git Checkout
        uses: actions/checkout@v2

      - name: Test proxies
        run: |
          cd tools
          .\meta_speedtest.exe --config ..\subs\merged\unlimited.yaml -o ..\subs\merged\elected.yaml --timeout 2000 --log-level debug

      - name: Git Add
        run: |
          git add subs/elected.yaml

      - name: Git Commit
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git commit -m "Updated by GitHub Action"
          git push